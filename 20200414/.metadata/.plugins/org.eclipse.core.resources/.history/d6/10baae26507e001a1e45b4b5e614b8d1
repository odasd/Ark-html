<!DOCTYPE html>
<html>
<head>
   <meta charset="UTF-8">
   <title>ㅋㅋ</title>
   <script>
      //경고창을 띄운다.
      alert("Hello Javascript");
      
      //Confirm 창을 띄운다.
      if(confirm("저장하시겠습니까?")){
         alert("저장");
      }esle{
         alert("취소")
      };
      /*
      var   radius = 10;
      var   pi = 3.14;
      var str = '원의 넓이=';
      var area = radius * radius * pi;
      alert(str + area);
      */
   /*  
      var hour = 9;
      switch(hour){
         case 9:
            alert("출근");
            break;
         case 12:
            alert("점심");
            break;
         case 18:
            alert("퇴근");
            break;
         default:
            alert("근무");
      }
      
      if(hour==9){
         alert("출근");
      }else if(hour==12){
         alert("점심");
      }else if(hour==18){
         alert("퇴근");
      }else{
         alert("근무");      
      }
      
      var sum = 0;
      for(i = 0; i<=100; i++){
         sum = sum + i;
      }
      alert("1~100까지의 합 = " + sum);
      
      var tot = 0;
      var j = 0;
      while(j <= 100){
         j = j+1 //1씩 증가한 갑을 j에 저장
         tot = tot+j // j값을 누적해서 tot에 저장
      }
      alert("1~100까지의 합 =" + tot);
      
      var array =[1,4,5,8,9];
      sum = 0;
      for(i = 0; i<array.length; i++){
         sum = sum + array[i];
      }
      alert("배열의 합계=" + sum);
      */
   </script>
</head>
<body>
   
</body>
</html>